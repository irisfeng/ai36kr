name: daily-digest

# 每日 08:00（北京时间）向已确认订阅者发送「今日 AI 一页」邮件
# GitHub 定时调度在高负载时会延迟甚至丢弃运行，故设 3 个时点（北京 08:13 / 09:13 / 10:13）：
# 首个实际运行的时点发送，send-daily.mjs 以 digest_log 幂等，其余时点自动跳过
on:
  schedule:
    - cron: '13 0,1,2 * * *'  # 00:13/01:13/02:13 UTC = 北京 08:13/09:13/10:13
  workflow_dispatch:

permissions:
  contents: write

# 与 aggregate 共用同一并发组：两个 workflow 都会 push main，串行化避免
# GitHub 延迟释放的定时运行成批撞车（non-fast-forward）
concurrency:
  group: main-writer
  cancel-in-progress: false

jobs:
  send:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: 发送日报邮件
        run: node scripts/send-daily.mjs
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      # 推广步骤一律 continue-on-error + 缺 secret 静默跳过，绝不影响日报主流程
      - name: SEO 推送（百度主动推送 + IndexNow）
        run: node scripts/push-urls.mjs
        continue-on-error: true
        env:
          BAIDU_PUSH_TOKEN: ${{ secrets.BAIDU_PUSH_TOKEN }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      - name: X 每日一推
        run: node scripts/post-x.mjs
        continue-on-error: true
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      - name: 生成今日头条海报
        run: node scripts/daily-card.mjs
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      - name: 导出内容快照（main 分支每日一次：冷启动水合 / 灾难恢复）
        run: node scripts/export-snapshot.mjs
        env:
          TURSO_DATABASE_URL: ${{ secrets.TURSO_DATABASE_URL }}
          TURSO_AUTH_TOKEN: ${{ secrets.TURSO_AUTH_TOKEN }}

      - name: 提交海报与每日快照
        run: |
          git config user.name "tidewire-bot"
          git config user.email "tidewire-bot@users.noreply.github.com"
          git add public/daily-card.html data/snapshot.json
          if git diff --cached --quiet; then
            echo "海报无变化"
          else
            git commit -m "chore(card): 今日头条海报 $(date -u +'%Y-%m-%d')"
            # 跨 workflow 并发组外的残余竞争兜底：rebase 后重试推送
            for i in 1 2 3; do
              git pull --rebase origin main && git push && exit 0
              sleep 5
            done
            exit 1
          fi
